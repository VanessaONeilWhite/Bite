<nav class="navbar navbar-expand-lg bg-#F7FFF7">
  <div id="nav-logo">
  <img src="https://res.cloudinary.com/dentfkaun/image/upload/v1655835814/My_project_v8pkzc.png">
  </div>
  <div class="container-fluid">
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        {% if current_user.is_authenticated %}
        <a class="nav-link" href="{{url_for('main.index')}}">Home</a>
        <a class="nav-link" href="{{url_for('main.bite')}}">Grab a bite</a>
        <a class="nav-link" href="{{url_for('main.my_bites')}}">My Bites</a>
        
        {% else %}
        <a class="nav-link" href="{{url_for('auth.login')}}">Login</a>
        <a class="nav-link" href="{{url_for('auth.register')}}">Register</a>
        {% endif %}
      <div>
        {% if current_user.is_authenticated %}
        <ul class="navbar-nav ms-auto align-items-center">
        <li class="nav-item"> 
        <div class="row">
        <div class="col-3">
            <a class="nav-link" id="icons"  href="{{url_for('auth.edit_profile')}}"><i class="bi bi-pen-fill"></i></a>
        </div>
        <div class="col-3">
            <a class="nav-link" id ="icons" href="{{url_for('auth.logout')}}"><i class="bi bi-door-closed-fill"></i></a>
        </div>
        </div>
            
        </li>
        </ul>
        {% endif %}
      </div>
    </div>
  </div>
</nav>